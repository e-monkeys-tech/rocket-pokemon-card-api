table! {
    users (id) {
        id -> Uuid,
        username -> Varchar,
        is_active -> Bool,
    }
    pokemon_cards (id) {
        id -> Uuid,
        username -> Varchar,
        is_active -> Bool,
    }
}
